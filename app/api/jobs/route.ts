import { NextResponse } from "next/server";
import { createClient } from "@/lib/supabase/server";
export async function GET(request:Request){const params=new URL(request.url).searchParams;const search=params.get("search")?.trim();const location=params.get("location")?.trim();const level=params.get("level");const workType=params.get("workType");if(!process.env.NEXT_PUBLIC_SUPABASE_URL||!process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY)return NextResponse.json({jobs:[],source:"fallback"});const supabase=await createClient();let query=supabase.from("jobs").select("*,companies(id,name,logo_url)").eq("status","published").order("created_at",{ascending:false});if(search)query=query.or(`title.ilike.%${search}%,description.ilike.%${search}%,industry.ilike.%${search}%`);if(location)query=query.ilike("location",`%${location}%`);if(level)query=query.eq("level",level);if(workType)query=query.eq("work_type",workType);const result=await query;if(result.error)return NextResponse.json({error:"Lowongan gagal dimuat"},{status:502});return NextResponse.json({jobs:result.data,source:"supabase"});}
