import { NextResponse } from "next/server";
import { createClient } from "@/lib/supabase/server";
export async function GET(_:Request,{params}:{params:Promise<unknown>}){const value=await params;const id=typeof value==="object"&&value!==null&&"id" in value&&typeof value.id==="string"?value.id:"";if(!id)return NextResponse.json({error:"ID lowongan tidak valid"},{status:400});if(!process.env.NEXT_PUBLIC_SUPABASE_URL||!process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY)return NextResponse.json({error:"Supabase belum dikonfigurasi"},{status:503});const supabase=await createClient();const result=await supabase.from("jobs").select("*,companies(id,name,logo_url)").eq("id",id).eq("status","published").single();if(result.error)return NextResponse.json({error:"Lowongan tidak ditemukan"},{status:404});return NextResponse.json({job:result.data,source:"supabase"});}
