import { NextResponse } from "next/server";
import { createClient } from "@/lib/supabase/server";
import { analyticsEventSchema } from "@/lib/schemas";
export async function POST(request:Request){let body:unknown;try{body=await request.json()}catch{return NextResponse.json({error:"Body JSON tidak valid"},{status:400})}const parsed=analyticsEventSchema.safeParse(body);if(!parsed.success)return NextResponse.json({error:"Event analytics tidak valid"},{status:400});if(!process.env.NEXT_PUBLIC_SUPABASE_URL||!process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY)return NextResponse.json({accepted:true,source:"local"},{status:202});const supabase=await createClient();const {data:{user}}=await supabase.auth.getUser();const result=await supabase.from("analytics_events").insert({user_id:user?.id??null,event_name:parsed.data.eventName,properties:parsed.data.properties}).select("id,event_name,created_at").single();if(result.error)return NextResponse.json({error:"Event analytics gagal disimpan"},{status:502});return NextResponse.json({event:result.data},{status:201});}
