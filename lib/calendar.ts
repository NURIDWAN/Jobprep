export function toIcsEvent(input:{uid:string;title:string;startsAt:string;durationMinutes:number;description?:string;url?:string}){const start=new Date(input.startsAt);const end=new Date(start.getTime()+input.durationMinutes*60000);const format=(date:Date)=>date.toISOString().replace(/[-:]/g,"").replace(/\.\d{3}/,"");const escape=(value:string)=>value.replace(/[,;\\]/g,"\\$&").replace(/\n/g,"\\n");return ["BEGIN:VCALENDAR","VERSION:2.0","PRODID:-//JobPrep//Interview//EN","BEGIN:VEVENT",`UID:${escape(input.uid)}`,`DTSTAMP:${format(new Date())}`,`DTSTART:${format(start)}`,`DTEND:${format(end)}`,`SUMMARY:${escape(input.title)}`,`DESCRIPTION:${escape(input.description??"")}`,input.url?`URL:${escape(input.url)}`:"","END:VEVENT","END:VCALENDAR"].filter(Boolean).join("\r\n");}
